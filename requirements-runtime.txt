# Project Laddu Clean Core R3 runtime lock.
# Exact versions only: a release identity must never resolve different runtime
# dependency versions on different install dates.
requests==2.34.2
upstox-python-sdk==2.28.0
duckdb==1.5.5

# Dedicated PostgreSQL operational and governance authorities.
psycopg==3.3.4
psycopg-binary==3.3.4
psycopg-pool==3.3.1
