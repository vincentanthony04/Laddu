# R8 Production Usability Closure

R8 is a product-closure descendant of the exact R7 full-product baseline. It remains one v131 runtime and one frontend on `http://127.0.0.1:8086/`; port 8787 is not used.

## Closure

- Durable Research projection now retains identity, first/latest seen, setup, score, holding period and latest reason, and exposes complete counterfactual Research metrics.
- Research, canonical Final Decision and actual Model Paper performance are visibly separate.
- Actionable rows expose LTP/change, frozen Entry/Target/SL/R:R, Signal Age, Holding Period, lifecycle/outcome and P&L on one contained line.
- Research history is filterable and paginated; reranked candidates remain visible.
- Both scanner cadences expose state, last/next cycle, countdown and heartbeat age; healthy sleep remains healthy.
- Stock Intelligence retains all ten canonical timeframes and fail-closed evidence behavior.
- Exact build identity is `v131 · R8 · 8086` with R8-only cache keys.
- `RUN_FINAL_PRODUCT_ACCEPTANCE.cmd` validates the installed package/browser, all customer destinations, console/network health, direct routes, Research refresh survival, service restart persistence and post-restart identity, then emits one evidence ZIP.

## Frozen authority

Every R7 backend byte is frozen except `backend/core/persistent_research_history_service.py`. Decision, geometry, costs, settlement/outcome, intraday/session and evidence mathematics remain byte-identical. Broker authority remains `NONE`; release state remains `NOT ACCEPTED / NOT RELEASE` until exact installed proof passes.
