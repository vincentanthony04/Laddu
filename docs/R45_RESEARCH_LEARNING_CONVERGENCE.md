# R45 Research Learning Convergence

R45 is a narrow backend research-learning convergence correction on exact R44.

It does not change Workspace, chart, scanner mathematics, DecisionRecord/risk, broker authority, database schema, or installer task ownership.

## Corrections

1. The autonomous historical PIT service refreshes the canonical read-only research catalogue before the existing Delivery 10-day historical trainer.
2. After market close, a deep historical training run is not terminated merely because a PostgreSQL pool is briefly fully occupied when there are no current waiters and no authority recovery. Manual/interactive priority, current DB waiters/recovery/unusable authority, scanner saturation, and market-hours P5 priority remain fail-safe yield conditions.
3. Scheduled dual-desk LightGBM shadow cycles use the worker's declared single-specification contract (`trial_count=1`).
4. Dual-desk research cycles re-evaluate immediately when either PIT labels or PIT snapshots advance; cadence suppresses only unchanged evidence.
5. A rejected/non-eligible LightGBM record cannot be projected as an available learned ranker.

## Explicit limits

R45 does not fabricate historical selector populations or weaken the 252-day WFA qualification floor. The canonical Delivery historical trainer remains Delivery-only. Intraday learned models remain the separate dual-desk shadow path. ML production influence remains zero until governed forward evidence validates alpha.
