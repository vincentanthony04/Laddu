Project Laddu v131.1.6 — R8 / PL46 SIMPLE INSTALL R6 + CLOSED-MARKET LINEAGE + UI7


IMPORTANT — CLEAN EXTRACTION REQUIRED
This archive is version-isolated. Use Windows "Extract All" and run INSTALL_UPDATE.cmd only from the ProjectLaddu-v131.1.6 folder created by this archive. Do NOT extract/overwrite into any older Project Laddu package folder. A mixed package tree fails closed before any installed-product mutation.

INSTALLATION CANDIDATE — NOT ACCEPTED — NOT A RELEASE

This installation candidate advances the sealed PL46 line with Research Terminality R5, Runtime Retry R4, QuestDB Recovery R3 and the UI7 actionable redesign, installing one v131.1.6 application at http://127.0.0.1:8086/. It preserves the protected trading/WFA/risk/cost mathematics while closing PIT-lineage repair, three-arm self-repair, semantic monitoring blockers, immutable lifecycle snapshots, actionable route diagnostics, and verified resumable corporate-action acquisition. The package metadata, frontend cache identity, exact inventory, and PL46 source-QC routing are sealed together in this micro-build.

After installation, run RUN_FINAL_PRODUCT_ACCEPTANCE.cmd as Administrator. It checks the installed package and browser before and after a real ProjectLaddu service restart, verifies Research refresh/restart persistence, and writes one evidence ZIP plus SHA256 sidecar.

Not claimed until exact installed evidence proves it: current provider/live-market behavior, a same-decision Actionable-to-settlement lifecycle, and any production-broker authority.

Broker authority remains NONE / Model Paper only. Existing production data and secure state are preserved by the installer contract. R8 remains NOT ACCEPTED / NOT RELEASE until the exact installed acceptance gate passes.


SIMPLE INSTALL R6
This candidate closes two field-proven P0 blockers without changing trading/WFA/ML mathematics: QuestDB retained-volume recovery now enforces exactly one live Project-Laddu owner before candidate start/promotion and fails fast on the QuestDB table-name-registry lock signature; closed-market completed-session quote consumption preserves historical source_as_of but stamps a fresh local received_at for the current research capture. Unknown containers on the retained QuestDB volume fail closed and are never stopped.
